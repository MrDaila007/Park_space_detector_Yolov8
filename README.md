# Parking Detector v2.0

## Описание

Продвинутая система компьютерного зрения для детекции парковочных мест в реальном времени с возможностью редактирования, удаления и интеллектуального анализа занятости.

## Основные возможности

- 🚗 **Универсальное распознавание объектов** с возможностью переключения в режим только автомобилей
- 📍 **Интерактивное создание парковочных мест** с визуальной обратной связью
- ✏️ **Редактирование парковочных мест** с возможностью перерисовки
- 🗑️ **Удаление парковочных мест** одним кликом
- 🧠 **Интеллектуальный анализ занятости** с поддержкой пограничных состояний
- ⏱️ **Отслеживание времени** свободности и занятости
- 🔄 **Частые срабатывания** - определение предполагаемой занятости
- 💾 **Централизованная конфигурация** с автоматическим сохранением
- 📊 **Статистика в реальном времени** с форматированием времени

## Структура проекта

```
ParkingDetector_v2/
├── src/
│   └── parking_detector.py      # Основной файл программы
├── config/
│   └── settings.json            # Конфигурация системы
├── data/
│   └── parking_spaces.json      # Парковочные места
├── models/
│   └── yolov8n.pt              # YOLO модель (загружается автоматически)
├── docs/
│   └── README_Detailed.md      # Подробная документация
├── examples/
│   ├── free_space.png          # Примеры изображений
│   ├── image_test_free.png
│   └── image_test_not_free.png
├── scripts/
│   └── install.sh              # Скрипт установки
├── requirements.txt            # Зависимости Python
└── README.md                   # Этот файл
```

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск программы

```bash
cd src
python parking_detector.py
```

### 3. Настройка видеопотока

Отредактируйте путь к видео в файле `src/parking_detector.py`:

```python
# Локальное видео
video_path = '/path/to/your/video.mp4'

# Веб-камера
video_path = 0

# IP-камера
video_path = 'http://your-camera-ip/stream'
```

### 4. Настройка устройства обработки

#### Автоматическое определение (по умолчанию):
```json
{
  "device": "auto",
  "force_cpu": false
}
```

#### Принудительное использование CPU:
```json
{
  "device": "cpu",
  "force_cpu": true
}
```

#### Принудительное использование CUDA:
```json
{
  "device": "cuda",
  "force_cpu": false
}
```

## Управление

### Клавиатурные команды

| Клавиша | Действие | Описание |
|---------|----------|----------|
| `D` | Debug Mode | Переключение режима отладки |
| `E` | Edit Mode | Режим редактирования парковочных мест |
| `R` | Delete Mode | Режим удаления парковочных мест |
| `U` | Universal Mode | Переключение между универсальным и автомобильным режимом |
| `S` | Save | Сохранение парковочных мест |
| `C` | Clear | Сброс всех режимов |
| `Q` | Quit | Выход из программы |

### Режимы работы

#### 1. Обычный режим (создание мест)
- Кликайте по 4 углам каждого парковочного места
- Точки отображаются желтыми кружками с нумерацией
- После 4-й точки место автоматически создается

#### 2. Режим редактирования (клавиша E)
- Кликните по существующему парковочному месту
- Перерисуйте точки в новых позициях
- После 4-й точки место обновится

#### 3. Режим удаления (клавиша R)
- Кликните по любому парковочному месту для удаления
- Место удаляется мгновенно

#### 4. Режим отладки (клавиша D)
- Кликайте по детектированным объектам для добавления их классов в отслеживание
- Объекты подсвечиваются желтыми рамками

## Конфигурация

Все настройки хранятся в файле `config/settings.json`:

```json
{
  "universal_detection": true,
  "occupancy_threshold": 0.6,
  "uncertainty_threshold": 0.3,
  "uncertainty_time_threshold": 3.0,
  "frequent_detection_threshold": 10,
  "frequent_detection_window": 10.0,
  "tracked_objects": [2, 67]
}
```

### Параметры

- `universal_detection` - универсальное распознавание всех объектов
- `occupancy_threshold` - порог занятости (0.6 = 60%)
- `uncertainty_threshold` - порог пограничного состояния (0.3 = 30%)
- `uncertainty_time_threshold` - время для перехода в "вероятно занято" (секунды)
- `frequent_detection_threshold` - количество срабатываний для частых детекций
- `frequent_detection_window` - временное окно для частых срабатываний (секунды)
- `tracked_objects` - ID классов объектов для отслеживания (COCO dataset)
- `force_cpu` - принудительное использование CPU (true/false)
- `device` - устройство для обработки ("auto", "cuda", "cpu")

## Состояния парковочных мест

| Состояние | Цвет | Описание |
|-----------|------|----------|
| **Free** | 🟢 Зеленый | Свободно |
| **Occupied** | 🔴 Красный | Занято |
| **Uncertain** | 🟡 Желтый | Неопределенное состояние |
| **Probably Occupied** | 🟠 Оранжево-красный | Вероятно занято |
| **Frequent Detection** | 🟣 Пурпурный | Частые срабатывания |

## Системные требования

#### Минимальные требования:
- Python 3.8+
- 4GB RAM
- OpenCV 4.0+

#### Рекомендуемые требования:
- Python 3.9+
- 8GB+ RAM
- CUDA-совместимая видеокарта (для ускорения)
- SSD накопитель

## Установка

### Автоматическая установка

```bash
chmod +x scripts/install.sh
./scripts/install.sh
```

### Ручная установка

1. Клонируйте репозиторий
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите программу: `cd src && python parking_detector.py`

## Лицензия

MIT License

## Авторы

- Разработчик системы детекции парковочных мест
- Интеграция YOLO и OpenCV
- Реализация интерактивного редактирования

## Версия

v2.0 - Полная система с интеллектуальным анализом и управлением парковочными местами

---

**Примечание:** Для получения лучших результатов рекомендуется использовать качественное освещение и стабильную камеру. Система работает лучше всего с четко видимыми парковочными местами и объектами.
